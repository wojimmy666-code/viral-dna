# ViralDNA Design QA

## 验收基线

- 参考图：`docs/UI模板.png`
- 桌面实现截图：`docs/qa/implementation-report-1696x931-final.png`
- 平板截图：`docs/qa/implementation-tablet-820x900.png`
- 手机截图：`docs/qa/implementation-mobile-390x844.png`
- 对照状态：示例视频分析完成，报告“总览”已打开，元素替换后的 Prompt v2 已生成
- 桌面参考尺寸：1696 × 931
- 桌面实现尺寸：1696 × 931
- 密度归一：同一 CSS 视口、1:1 尺寸对照

## 视觉对照

参考图和桌面实现截图已在同一次对照输入中打开检查。实现保留了参考图的浅灰工作台画布、白色卡片、细边框、轻阴影、紫色主色、左侧导航、顶部工具栏、中央报告区和右侧 AI 建议栏。信息架构按一期边界收敛：以单视频导入与拆解报告替代账号、榜单和多视频数据库模块。

重点检查区域：

1. 左侧导航、顶部搜索与主操作按钮的层级和间距。
2. 导入卡片、报告标题、标签页和右侧建议栏的卡片语言。
3. 总览视频区、内容潜力评分、分镜结构条和信息密度。
4. 桌面、平板、手机下的折行、滚动、触控尺寸与横向溢出。

## 问题与修正记录

| 级别 | 发现 | 修正 | 结果 |
|---|---|---|---|
| P2 | 生成替换版后，报告标题仍显示 Prompt v1 | 报告标题改为读取当前 Prompt Package | 已修正，显示 Prompt v2 |
| P2 | Windows 下报告标签栏出现原生滚动条按钮 | 隐藏标签栏滚动条，保留触控横向滚动 | 已修正 |
| P3 | 一期页面没有参考图中的榜单与账号协作模块 | 属于明确的产品范围收敛，不作为缺陷 | 接受 |

## 功能与响应式验收

- 已跑通：示例分析 → SSE/轮询进度 → 报告总览 → 分镜切换 → 爆点证据 → 元素替换 → Prompt v2。
- 1696 × 931：桌面三栏结构稳定，参考图关键视觉语言一致。
- 820 × 900：侧栏收起，主内容完整，报告与导入区正常堆叠。
- 390 × 844：表单和主操作按钮单列展示，报告标签可横向滚动。
- 手机视口 `scrollWidth === clientWidth`，无页面级横向溢出。
- 浏览器未发现应用来源的 error 或 warning；记录到的 warning 均来自用户安装的浏览器扩展。
- 未使用手绘 SVG、CSS 图形或占位图片；界面图标统一使用 Phosphor Icons。

## 最终结论

P0：0；P1：0；P2：0（发现项均已修正）。

final result: passed

## Batch 4.5.1 方案默认画幅与分镜图片预览 QA

### 验收基线

- 视觉问题来源：`docs/qa/source-shot-preview-fixed-height.png`，1167 × 1090；该截图记录了横版关键帧被放入固定竖向高容器后产生大面积留白的问题。
- 浏览器实现截图：`docs/qa/implementation-shot-preview-16x9-scrolled.png`，1439 × 735。
- 默认画幅截图：`docs/qa/implementation-create-project-default-16x9.png`，1439 × 735。
- Chrome CSS 视口：1440 × 735；截图密度按 1 CSS px 对应约 1 图像 px 检查。来源截图是用户提供的局部问题截图，因此以相同组件状态做定性对照，不对浏览器外围区域做像素级比较。
- 实现状态：1280 × 720、16:9 的真实分析记录；现有创作方案输出为 16:9、1920 × 1080；分镜 1 已打开，AI 图片为空状态。

### 同屏对照与修正历史

参考截图与最终实现截图已在同一次对照输入中打开。来源截图中的两个预览框被固定为高竖框，横版关键帧只占中间一小条；最终实现由方案画幅驱动，两个预览框均为 16:9，横版关键帧完整填入目标画布且不裁剪。

| 级别 | 发现 | 修正 | 最终证据 |
|---|---|---|---|
| P1 | `.shot-media-frame` 使用固定高度，横版视频在竖向容器中产生大量无效留白 | 容器改为读取方案输出宽高比，并同时约束原始关键帧和 AI 图片画布 | 两个预览框实测均为 255 × 143，比例 1.778；关键帧 `object-fit: contain` |
| P1 | 新建方案前端固定预选 9:16，与 1280 × 720 源视频不一致 | 前端按源视频选择最接近的受支持画幅，后端缺省创建逻辑同步兜底 | 创建弹窗显示“已按源视频预选 16:9”，宽 1920、高 1080 |

### 必检视觉面

- 字体与排版：沿用现有 ViralDNA 字体栈、字号、字重与截断规则；本次没有引入新的字体或层级漂移。
- 间距与布局：两列对比结构、标题、按钮和卡片间距保持不变，只消除固定竖向高度；横版画布不再挤压后续生图设置区域。
- 颜色与视觉令牌：继续使用现有紫色选中态、浅灰画布、细边框和圆角令牌，没有新增不一致颜色或阴影。
- 图片质量：源关键帧自然尺寸 640 × 360，使用 `contain` 完整显示；没有拉伸、裁剪、模糊放大或伪造占位素材。
- 文案与内容：新增默认画幅说明，明确“按源视频预选”且允许手动调整；宽高输入与选中画幅一致。

### 功能与浏览器检查

- 已验证：分析记录 → 创作方案 → 打开新建方案弹窗 → 检查默认 16:9 / 1920 × 1080 → 取消 → 打开现有方案 → 分镜图片 → 分镜 1 / 2 往返切换。
- 分镜切换后仍保持 16:9；活动分镜正确回到“分镜 1”。
- 浏览器未出现 Vite 错误覆盖层、可见错误提示或交互异常；应用运行态未观察到 console/runtime error。
- 桌面宽度下两图并排；既有 440px 容器断点继续负责窄屏单列堆叠。

### 自动化结果

- 后端完整回归测试：119 / 119 通过。
- Web 单元测试：29 / 29 通过。
- Ruff：通过。
- Web 生产构建：通过；仅保留既有的 Vite 大包体积提示。

### 最终结论

P0：0；P1：0（两项均已修正）；P2：0。

final result: passed

## Batch 4.4.3 资产库视觉与交互 QA

### 对照基线

- 结构参考：用户提供的深色资产管理侧栏截图，381 × 733。
- 实现状态：ViralDNA 本地开发环境资产库空状态，1425 × 783。
- 对照方式：参考图与实现截图已合并到同一张对照图中检查。
- 视觉策略：保留参考图的“目录 / 资产缩略图 / 搜索”信息架构，视觉语言服从 ViralDNA 既有浅色紫色设计系统。

### 视觉检查

- 左侧一级目录、未分类入口、目录数量与新建入口层级清晰。
- 搜索、类型、存储状态、归档筛选保持同一工具栏，并使用现有表单样式。
- 空状态、上传弹窗和本地存储状态均有明确反馈，没有伪造素材缩略图。
- 上传弹窗在同一视图中完成文件预览、元数据、目录和权利确认。
- 桌面端主内容与现有 232px 应用导航对齐；窄屏下目录转为横向列表，详情面板转为覆盖层。
- 820 × 900：目录与资产浏览区纵向堆叠，页面无横向溢出。
- 390 × 844：筛选器和主操作单列展示，页面无横向溢出。

### 功能检查

- 已验证：进入资产库、创建目录、清理测试目录、打开上传表单、类型筛选、归档筛选、刷新列表。
- 浏览器控制台 error：0。
- 临时验收数据已清理，工作区保持验收前的空资产状态。
- 上传、编辑、移动、归档、恢复、分页和对象内容读取由 API 集成测试覆盖。

### 最终结论

P0：0；P1：0；P2：0。

final result: passed

## Batch 4.4.3 资产库体验修正 QA

### 验收范围

- 新建 / 重命名目录弹窗改为桌面 460px 的紧凑弹窗，窄屏宽度保留 16px 双侧安全间距。
- 上传成功后关闭上传弹窗、清空冲突筛选、进入实际目标目录并高亮新资产，不自动打开详情面板。
- 资产卡片使用 1:1 预览区和 `contain`，详情使用原图内容地址与自适应比例，竖版图片完整展示。
- 资产库顶栏仅保留通知与帮助，移除仅服务分析记录的全局搜索和“新建分析”。

### 实机检查

- 环境：本地开发站点 `http://127.0.0.1:4174`，Chrome，1425px 桌面视口与 390 × 844 窄屏视口。
- 数据：工作区真实竖版资产 `LC12`，3024 × 4032。
- 卡片预览：218 × 218，计算样式 `object-fit: contain`，人物与画面上下边界完整可见。
- 详情预览：287 × 383，计算样式 `object-fit: contain`，图片源为原图 `/content`，未使用列表缩略图放大。
- 目录弹窗：桌面实测宽度 460px；窄屏未超出视口。
- 顶栏：资产页 `.global-search` 数量为 0，顶栏主按钮数量为 0；通知和帮助保留。
- 上传后落点由纯函数单元测试覆盖：目标目录、搜索、类型、存储状态、归档筛选和页码均回到可见的新资产列表状态。
- 应用来源的浏览器 error：0；现有 warning 均来自用户安装的浏览器扩展。

### 自动化结果

- Web 单元测试：23 / 23 通过。
- Web 生产构建：通过；仅保留既有的 Vite 大包体积提示。

### 最终结论

P0：0；P1：0；P2：0。

final result: passed
