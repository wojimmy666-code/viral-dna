# ViralDNA Design QA

## 2026-09-13 对外官网首页（当前验收）

### 对照基线与范围

- 用户批准稿：`.impeccable/mocks/cinematic-negative-space.png`，1536 × 1024。
- 用户明确同意采用当前 1672 × 941 原生主视觉完成本地版本，不放大补像素；影像为设计示意，不作为真实客户案例。
- 实现：`apps/web/src/landing/HomePage.jsx`，本地预览 `http://127.0.0.1:5175/`。
- 比较状态：匿名官网、首张“产品特写”、页面顶部；用真实控件暂停轮播，因此右下角显示播放图标。首屏截图为 1536 × 1024、DPR 1；完整桌面为 1440px 宽，手机为 390 × 844。另检查 320、375、768、1024、1280、1600px 宽度。
- 浏览器：用户授权的独立无头 Chrome、新建临时配置；没有使用个人浏览器资料。真实生产构建由临时回环 HTTP 服务验收，登录响应是合成 fixture，不连接真实账户或生成任务。
- 最终证据目录：`.impeccable/review/`。`hero-repro.png` 是同尺寸首屏，`desktop.png` 与 `mobile.png` 是全页；菜单、弹窗和其他宽度另有具名截图。

### 对照与修正历史

| 发现 | 处理 | 验证范围 |
| --- | --- | --- |
| 首轮导航及双入口位置、字号与稿件有偏移 | 按同屏与局部对照调整真实布局 | 首屏整体和导航/标题/缩略图局部 |
| Esc 与弹窗焦点恢复不可靠 | 处理原生 cancel/close 和 Escape，并在清理时关闭 dialog 后恢复焦点 | 真实键盘浏览器测试 |
| 手机团队标题拆开“同一个”，暂停与菜单点击区域偏小 | 只调整换行位置，按钮提升至 44px | 320–820px 布局 |
| 首次全页截图时轮播已切到第二张 | 截图前用真实控件选择“产品特写”并保持暂停，断言选中状态 | 已重拍有效首屏及全页；旧的错误轮播状态不作为证据 |
| Chromium 截图/帧等待超时 | 修正隔离浏览器的活动标签与帧调度；不改页面设计 | 官网浏览器 6 项全部通过，最终 13 张截图有效 |

### 五项忠实度检查

- 字体：标题自托管 Noto Sans SC 子集，正文保留中文系统字体；大标题两行、正常字距。原工作台字号令牌未修改，官网令牌仅在 `.vd-home` 内生效。
- 布局：深色全幅主视觉、左侧标题和操作、右下角三张缩略图；下方按双入口、流程、创作方法、团队和收尾组织。手机独立重排，不缩小整张桌面稿。
- 颜色：炭黑、白字和既有品牌紫；暖金来自影像。无虚构客户标识或数据。
- 图片：四张独立生成的干净素材，不使用整张界面截图充当网页；WebP 优先、PNG 回退、错误状态可见；所有素材带提示词记录。
- 文案：首屏沿用批准稿；下方只描述现有流程。静态分镜演示、创作方向、协作示意均明确标记，不读取私有目录或伪装实际运行状态。

### 自动化与边界

- 工作台主回归 312 项通过；新增主页和登录目标纯函数 6 项通过。
- 账户浏览器回归 14 项通过，包含独立登录、只读/编辑租约及返回公开首页时释放编辑锁。
- 官网浏览器验收 6 项通过，覆盖匿名请求边界、切图与暂停、弹窗 Escape/焦点恢复、流程键盘操作、响应式、减少动态效果、缺图回退与合成登录返回。文档检查通过（86 个索引文件、315 个本地引用）。
- 官网常规状态没有账户 API 或工作台主代码请求；控制台检查覆盖公开页面交互。缺图 404 和合成登录后真实 Skill 后端行为不计为生产后端验证。
- 生产构建通过；首页入口 JS gzip 约 85 KB、CSS gzip 约 4 KB，工作台主包延迟加载。既有工作台大包警告保留，不属于此次首页首载。
- impeccable 检测器单次结果为 `[]`；素材扫描 8 个栅格、0 项来源缺失。
- 最终同尺寸像素对比为 81%，但局部新图岩石纹理、图标/滚动条仍有差异，机械门禁未全部闭合；保留原始报告与状态，不宣称自动像素门禁通过。`scene-label`、`thumb-close` 和 `thumb-scene` 的配对局部截图已打开检查，标签和素材并未缺失。按技能降级规则使用同尺寸视觉对照及独立 reviewer，不能用构建通过替代视觉验收。

### 当前结论

独立 finish reviewer 已返回 `disposition: ship`，完整五部分结论见 `.impeccable/review/finish-review.md`。复核覆盖全部 13 张截图、15 个配对局部及产品边界，没有必须返工的问题；结论仅适用于获准的原生素材本地版本，不代表机械门禁全通过或真实后端已联调。

独立 documenter 已完成 `apps/web/src/landing/DESIGN.md`（真实令牌 frontmatter）和邻近 `.impeccable/design.json`（schemaVersion 2、5 个代表组件）；JSON、令牌和文档引用校验通过。旧工作台规范正文保留，仅补充官网隔离边界；用户偏好及公开路由规则已写入 `apps/web/AGENTS.md`。尚未部署、推送或修改真实账户数据。

final result: passed（仅本地验收；上述机械门禁、素材规格与真实后端范围例外保留）

---

以下为此前工作台功能的历史验收记录，不代表本次官网的验收结果。

## 验收基线

- 参考图：`docs/design/UI模板.png`
- 桌面实现截图：`docs/qa/screenshots/implementation-report-1696x931-final.png`
- 平板截图：`docs/qa/screenshots/implementation-tablet-820x900.png`
- 手机截图：`docs/qa/screenshots/implementation-mobile-390x844.png`
- 对照状态：示例视频分析完成，报告“总览”已打开，元素替换后的 Prompt v2 已生成
- 桌面参考尺寸：1696 × 931
- 桌面实现尺寸：1696 × 931
- 密度归一：同一 CSS 视口、1:1 尺寸对照

## 视觉对照

参考图和桌面实现截图已在同一次对照输入中打开检查。实现保留了参考图的浅灰工作台画布、白色卡片、细边框、轻阴影、紫色主色、左侧导航、顶部工具栏、中央报告区和右侧 AI 建议栏。信息架构按一期边界收敛：以单视频导入与拆解报告替代账号、榜单和多视频数据库模块。

重点检查区域：

1. 左侧导航、顶部搜索与主操作按钮的层级和间距。
2. 导入卡片、报告标题、标签页和右侧建议栏的卡片语言。
3. 总览视频区、内容潜力评分、分镜结构条和信息密度。
4. 桌面、平板、手机下的折行、滚动、触控尺寸与横向溢出。

## 问题与修正记录

| 级别 | 发现 | 修正 | 结果 |
|---|---|---|---|
| P2 | 生成替换版后，报告标题仍显示 Prompt v1 | 报告标题改为读取当前 Prompt Package | 已修正，显示 Prompt v2 |
| P2 | Windows 下报告标签栏出现原生滚动条按钮 | 隐藏标签栏滚动条，保留触控横向滚动 | 已修正 |
| P3 | 一期页面没有参考图中的榜单与账号协作模块 | 属于明确的产品范围收敛，不作为缺陷 | 接受 |

## 功能与响应式验收

- 已跑通：示例分析 → SSE/轮询进度 → 报告总览 → 分镜切换 → 爆点证据 → 元素替换 → Prompt v2。
- 1696 × 931：桌面三栏结构稳定，参考图关键视觉语言一致。
- 820 × 900：侧栏收起，主内容完整，报告与导入区正常堆叠。
- 390 × 844：表单和主操作按钮单列展示，报告标签可横向滚动。
- 手机视口 `scrollWidth === clientWidth`，无页面级横向溢出。
- 浏览器未发现应用来源的 error 或 warning；记录到的 warning 均来自用户安装的浏览器扩展。
- 未使用手绘 SVG、CSS 图形或占位图片；界面图标统一使用 Phosphor Icons。

## 最终结论

P0：0；P1：0；P2：0（发现项均已修正）。

final result: passed

## Batch 4.7 分析记录生命周期与高密度列表 QA

### 验收基线

- 选定方案：方案 B，高密度单列记录列表。
- 视觉参考：`docs/design/analysis-records-option-b-reference.png`。
- 实现对照：`docs/qa/screenshots/analysis-records-option-b-implementation.png`，并在本地 Chrome 中复核最终标题收敛版本。
- 桌面视口：1426 × 773；平板视口：768 × 900；手机视口：390 × 844。

### 问题与修正

| 级别 | 发现 | 修正 | 结果 |
|---|---|---|---|
| P2 | 主标题和列表上方重复显示“当前记录 / 结果数”，压缩首屏列表空间 | 将结果数和目录筛选状态合并到唯一主标题旁，删除重复结果标题行 | 表格顶部由约 334px 降至约 274px |
| P2 | 大量记录需要明确区分正常使用、暂存与待删除状态 | 增加当前记录、已归档、回收站三个独立生命周期入口及数量 | 桌面嵌套导航、窄屏正文分段栏均已验证 |
| P2 | 卡片网格不利于批量管理 | 改为始终显示复选框的单列列表，并在选中后显示底部批量操作栏 | 三类生命周期均显示正确操作集合 |
| P2 | 进入分析记录时，非静默刷新卸载列表且缩略图从骨架重新淡入，导致全部图片同步闪动 | 已有记录时保留列表、记住会话内已加载 URL，并让骨架和图片交叉淡入 | 重复进入采样中表格始终存在，4 张图片保持 loaded，骨架透明度均为 0 |

### 浏览器检查

- 桌面最终表格顶部约 274px，记录行高 86px；当前 4 条记录和分页可在 773px 高视口内完整显示。
- 768px 与 390px 视口下均满足 `document.scrollWidth <= innerWidth`，没有页面级横向溢出。
- 390px 视口中的批量操作栏实测左右边界约为 15px 与 360px，完整位于 390px 视口内。
- 已归档批量栏显示“恢复到当前记录 / 移入回收站”；回收站显示“恢复 / 永久删除”。
- 回收站记录详情入口为禁用状态，避免在待删除状态继续进入编辑流程。
- 实机未执行永久删除，避免破坏用户现有记录；破坏性流程由 API 定向测试和人工验收步骤覆盖。

### 自动化结果

- Web 单元测试：57 / 57 通过。
- Web 生产构建：通过；仅保留既有的大包体积提示。
- API 全量回归受本机失效 Python 3.11 虚拟环境阻塞，已记录在 Batch 4.7 执行验收文档中，不作为视觉 QA 缺陷隐藏。

### 最终结论

设计与响应式 QA：P0 0；P1 0；P2 0（发现项均已修正）。

final result: passed

## Batch 4.5.1 方案默认画幅与分镜图片预览 QA

### 验收基线

- 视觉问题来源：`docs/qa/screenshots/source-shot-preview-fixed-height.png`，1167 × 1090；该截图记录了横版关键帧被放入固定竖向高容器后产生大面积留白的问题。
- 浏览器实现截图：`docs/qa/screenshots/implementation-shot-preview-16x9-scrolled.png`，1439 × 735。
- 默认画幅截图：`docs/qa/screenshots/implementation-create-project-default-16x9.png`，1439 × 735。
- Chrome CSS 视口：1440 × 735；截图密度按 1 CSS px 对应约 1 图像 px 检查。来源截图是用户提供的局部问题截图，因此以相同组件状态做定性对照，不对浏览器外围区域做像素级比较。
- 实现状态：1280 × 720、16:9 的真实分析记录；现有创作方案输出为 16:9、1920 × 1080；分镜 1 已打开，AI 图片为空状态。

### 同屏对照与修正历史

参考截图与最终实现截图已在同一次对照输入中打开。来源截图中的两个预览框被固定为高竖框，横版关键帧只占中间一小条；最终实现由方案画幅驱动，两个预览框均为 16:9，横版关键帧完整填入目标画布且不裁剪。

| 级别 | 发现 | 修正 | 最终证据 |
|---|---|---|---|
| P1 | `.shot-media-frame` 使用固定高度，横版视频在竖向容器中产生大量无效留白 | 容器改为读取方案输出宽高比，并同时约束原始关键帧和 AI 图片画布 | 两个预览框实测均为 255 × 143，比例 1.778；关键帧 `object-fit: contain` |
| P1 | 新建方案前端固定预选 9:16，与 1280 × 720 源视频不一致 | 前端按源视频选择最接近的受支持画幅，后端缺省创建逻辑同步兜底 | 创建弹窗显示“已按源视频预选 16:9”，宽 1920、高 1080 |

### 必检视觉面

- 字体与排版：沿用现有 ViralDNA 字体栈、字号、字重与截断规则；本次没有引入新的字体或层级漂移。
- 间距与布局：两列对比结构、标题、按钮和卡片间距保持不变，只消除固定竖向高度；横版画布不再挤压后续生图设置区域。
- 颜色与视觉令牌：继续使用现有紫色选中态、浅灰画布、细边框和圆角令牌，没有新增不一致颜色或阴影。
- 图片质量：源关键帧自然尺寸 640 × 360，使用 `contain` 完整显示；没有拉伸、裁剪、模糊放大或伪造占位素材。
- 文案与内容：新增默认画幅说明，明确“按源视频预选”且允许手动调整；宽高输入与选中画幅一致。

### 功能与浏览器检查

- 已验证：分析记录 → 创作方案 → 打开新建方案弹窗 → 检查默认 16:9 / 1920 × 1080 → 取消 → 打开现有方案 → 分镜图片 → 分镜 1 / 2 往返切换。
- 分镜切换后仍保持 16:9；活动分镜正确回到“分镜 1”。
- 浏览器未出现 Vite 错误覆盖层、可见错误提示或交互异常；应用运行态未观察到 console/runtime error。
- 桌面宽度下两图并排；既有 440px 容器断点继续负责窄屏单列堆叠。

### 自动化结果

- 后端完整回归测试：119 / 119 通过。
- Web 单元测试：29 / 29 通过。
- Ruff：通过。
- Web 生产构建：通过；仅保留既有的 Vite 大包体积提示。

### 最终结论

P0：0；P1：0（两项均已修正）；P2：0。

final result: passed

## Batch 4.4.3 资产库视觉与交互 QA

### 对照基线

- 结构参考：用户提供的深色资产管理侧栏截图，381 × 733。
- 实现状态：ViralDNA 本地开发环境资产库空状态，1425 × 783。
- 对照方式：参考图与实现截图已合并到同一张对照图中检查。
- 视觉策略：保留参考图的“目录 / 资产缩略图 / 搜索”信息架构，视觉语言服从 ViralDNA 既有浅色紫色设计系统。

### 视觉检查

- 左侧一级目录、未分类入口、目录数量与新建入口层级清晰。
- 搜索、类型、存储状态、归档筛选保持同一工具栏，并使用现有表单样式。
- 空状态、上传弹窗和本地存储状态均有明确反馈，没有伪造素材缩略图。
- 上传弹窗在同一视图中完成文件预览、元数据、目录和权利确认。
- 桌面端主内容与现有 232px 应用导航对齐；窄屏下目录转为横向列表，详情面板转为覆盖层。
- 820 × 900：目录与资产浏览区纵向堆叠，页面无横向溢出。
- 390 × 844：筛选器和主操作单列展示，页面无横向溢出。

### 功能检查

- 已验证：进入资产库、创建目录、清理测试目录、打开上传表单、类型筛选、归档筛选、刷新列表。
- 浏览器控制台 error：0。
- 临时验收数据已清理，工作区保持验收前的空资产状态。
- 上传、编辑、移动、归档、恢复、分页和对象内容读取由 API 集成测试覆盖。

### 最终结论

P0：0；P1：0；P2：0。

final result: passed

## Batch 4.4.3 资产库体验修正 QA

### 验收范围

- 新建 / 重命名目录弹窗改为桌面 460px 的紧凑弹窗，窄屏宽度保留 16px 双侧安全间距。
- 上传成功后关闭上传弹窗、清空冲突筛选、进入实际目标目录并高亮新资产，不自动打开详情面板。
- 资产卡片使用 1:1 预览区和 `contain`，详情使用原图内容地址与自适应比例，竖版图片完整展示。
- 资产库顶栏仅保留通知与帮助，移除仅服务分析记录的全局搜索和“新建分析”。

### 实机检查

- 环境：本地开发站点 `http://127.0.0.1:4174`，Chrome，1425px 桌面视口与 390 × 844 窄屏视口。
- 数据：工作区真实竖版资产 `LC12`，3024 × 4032。
- 卡片预览：218 × 218，计算样式 `object-fit: contain`，人物与画面上下边界完整可见。
- 详情预览：287 × 383，计算样式 `object-fit: contain`，图片源为原图 `/content`，未使用列表缩略图放大。
- 目录弹窗：桌面实测宽度 460px；窄屏未超出视口。
- 顶栏：资产页 `.global-search` 数量为 0，顶栏主按钮数量为 0；通知和帮助保留。
- 上传后落点由纯函数单元测试覆盖：目标目录、搜索、类型、存储状态、归档筛选和页码均回到可见的新资产列表状态。
- 应用来源的浏览器 error：0；现有 warning 均来自用户安装的浏览器扩展。

### 自动化结果

- Web 单元测试：23 / 23 通过。
- Web 生产构建：通过；仅保留既有的 Vite 大包体积提示。

### 最终结论

P0：0；P1：0；P2：0。

final result: passed
