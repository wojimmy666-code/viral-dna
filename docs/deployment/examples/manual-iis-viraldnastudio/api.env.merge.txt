# Merge this setting into C:\Projects\ViralDNA\.server\config\api.env.
# This is NOT a complete api.env. Do not replace your existing file with it.
# Keep exactly one VIRAL_DNA_CORS_ORIGINS line. Preserve any other explicitly
# authorized origins already required by your deployment; do not use '*'.
# Keep all existing authentication, account/data paths and provider secrets.
VIRAL_DNA_CORS_ORIGINS=http://127.0.0.1:8080,http://127.0.0.1:18080,http://127.0.0.1:8081,https://viraldnastudio.com,https://www.viraldnastudio.com
