# 企业成员管理 · 独立收尾复核

日期：2026-09-13。审阅者：独立、无对话历史的 `impeccable_finish_reviewer_members`。范围仅限成员管理扩展；不等于真实账号操作或正式部署验收。

## 首次检查：fix

五张必需全页截图有效，桌面和手机内容完整。既有工作台的排版、材质、表面色、列表与操作符合契约；手机逐字段重排为合理适配，无需新视觉方向或素材。

两项修正：

1. 手机号冲突不应通过 `aria-invalid` / `aria-describedby` 关联到密码框；按姓名、手机号、密码和表单级区分。
2. 成功提交后，应等待列表刷新结束、按钮恢复可操作才归还焦点；不能在 `busy` 仍为真时仅尝试一次。

保留：紧凑布局、内联表单、失败草稿、清晰成员身份及数据保留说明。

## 修正复核：ship

| 修正 | 结果 | 证据 |
| --- | --- | --- |
| 错误关联 | resolved | 姓名、手机号、密码分别关联，表单级错误不误标密码；浏览器增加姓名必填、手机号冲突断言。 |
| 提交后焦点 | resolved | 等待表单关闭及 `busy=false`，移除后回退添加按钮；创建、移除均通过 200ms 延迟刷新焦点断言。 |

审阅者重新查看同五张截图有效，修正未引入可见布局回归。最终 `ship` 只覆盖以上两项修正的复核，未将该结论扩大成全系统或正式服务器认证。
