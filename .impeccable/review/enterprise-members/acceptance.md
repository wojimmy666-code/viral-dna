# 企业成员管理 · 本地验收

日期：2026-09-13。范围：企业负责人管理普通成员，普通成员和个人账户不开放管理；平台 admin 独立保留。本次为既有浅色工作台的功能扩展，无新视觉方向或生成素材。以下交付状态按验收时点记录，后续 Git 提交不代表已部署；截图仅保留在本地本目录，不随源码提交。

## 交付内容

- 直接创建：姓名、11 位独立手机号、至少 8 位初始密码，创建即启用。
- 当前／已移除列表、姓名和手机号搜索、创建时间；桌面表格，手机逐行重排。
- 软移除、设置新密码恢复同一用户、重置密码；确认目标清晰，失败保留草稿，提交中防重复。
- 保留用户 ID、企业归属、项目、资产、生成历史、署名和额度；撤销旧会话、邀请／重置链接、同步令牌和编辑租约。
- 旧邀请／激活接口与平台 admin 流程兼容；没有数据库结构迁移。

## 设计约束与证据

沿用 `apps/web/DESIGN.md`、`src/accounts/accounts.css` 与 `src/ui/system`，页面 20px、区块 16px、正文 14px、辅助文字 12px；手机输入和主要操作至少 44px 高。无首页视觉、品牌、账号切换或平台权限扩展。质量标准是权限可理解、单一创建入口、失败可恢复、手机无横向溢出。

隔离浏览器使用合成成员、手机号及故意超长的企业名称，不连接真实 API 或账号。截图：`account-members-1280.png`、`account-members-390.png`、`enterprise-member-create-error-1280.png`、`enterprise-member-create-error-390.png`、`enterprise-member-restore-390.png`。全页截图；没有批准的视觉 comp 或重建要求。

## 检查

- 后端账户输入、仓库及 HTTP 隔离：42 项通过；覆盖创建后登录、归属、跨租户和角色拒绝、CSRF、移除／恢复旧凭据失效、已有项目保留、邀请兼容。
- 完整 API 启动／首次及后续账户回归：2 项通过（临时数据）。
- Ruff 检查通过。
- `npm run check:design`：8 项通过。
- `npm run test:web`：预检查 75 项、主测试 313 项通过。
- 隔离账户浏览器：16 项通过（含原平台 admin、改号、存储回归）。
- 独立复核的字段错误关联、延迟刷新后焦点恢复两项均已修正并评分 `resolved`；两项复核范围的 disposition 为 `ship`，记录见 `finish-review.md`。修正后设计检查、前端测试、浏览器与构建再次通过。
- `npm run build:web`：通过，Sites 兼容构建文件保留。既有大型创作模块的分块警告仍在，不属于本次调整。
- impeccable changed-target detector：无发现（`[]`）。
- 文档校验：86 个文档、315 个本地链接／引用通过；`git diff --check` 通过。

不提交 GitHub、不部署正式服务器，不改真实账号或成员数据。测试机 API 未开启自动重载，重启及联调状态单独记录，不能把隔离测试当作正式环境验收。

## 测试机重启（2026-09-14）

用户确认后，重启了本项目 `127.0.0.1:8000` API，启动时间为北京时间 00:03:31，晚于成员接口代码更新。前端 `127.0.0.1:5175` 未重启；健康检查、前端成员页面和该来源 CORS 预检均为 200，认证仍为已初始化的 password 模式，未登录读取成员接口仍返回 401。OpenAPI 也需要登录，因此没有绕过认证或操作真实成员做验证。

未修改账户／资产数据、持久配置或正式服务器，未推送 GitHub。后续登录后的真实成员操作由用户验收。
